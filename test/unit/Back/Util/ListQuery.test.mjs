import assert from 'node:assert/strict';
import {test} from 'node:test';
import subject from '../../../../src/Back/Util/ListQuery.mjs';

test('Back/Util/ListQuery.mjs exposes its default unit contract', () => {
    assert.notEqual(subject, undefined);
});
