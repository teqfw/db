import assert from 'node:assert/strict';
import {test} from 'node:test';
import subject from '../../../../../../src/Back/Enum/Db/Type/Column.mjs';

test('Back/Enum/Db/Type/Column.mjs exposes its default unit contract', () => {
    assert.notEqual(subject, undefined);
});
