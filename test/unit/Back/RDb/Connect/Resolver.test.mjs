import assert from 'node:assert/strict';
import {test} from 'node:test';
import subject from '../../../../../src/Back/RDb/Connect/Resolver.mjs';

test('Back/RDb/Connect/Resolver.mjs exposes its default unit contract', () => {
    assert.notEqual(subject, undefined);
});
