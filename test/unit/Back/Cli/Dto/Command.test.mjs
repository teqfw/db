import assert from 'node:assert/strict';
import {test} from 'node:test';
import subject from '../../../../../src/Back/Cli/Dto/Command.mjs';

test('Back/Cli/Dto/Command.mjs exposes its default unit contract', () => {
    assert.notEqual(subject, undefined);
});
