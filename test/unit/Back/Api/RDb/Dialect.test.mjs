import assert from 'node:assert/strict';
import {test} from 'node:test';
import subject from '../../../../../src/Back/Api/RDb/Dialect.mjs';

test('Back/Api/RDb/Dialect.mjs exposes its default unit contract', () => {
    assert.notEqual(subject, undefined);
});
