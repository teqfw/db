import assert from 'node:assert/strict';
import {test} from 'node:test';
import subject from '../../../../../src/Back/Dto/Config/Schema.mjs';

test('Back/Dto/Config/Schema.mjs exposes its default unit contract', () => {
    assert.notEqual(subject, undefined);
});
