import assert from 'node:assert/strict';
import {test} from 'node:test';
import subject from '../../../../../src/Back/Dto/Config/Local.mjs';

test('Back/Dto/Config/Local.mjs exposes its default unit contract', () => {
    assert.notEqual(subject, undefined);
});
