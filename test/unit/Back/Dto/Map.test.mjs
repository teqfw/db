import assert from 'node:assert/strict';
import {test} from 'node:test';
import subject from '../../../../src/Back/Dto/Map.mjs';

test('Back/Dto/Map.mjs exposes its default unit contract', () => {
    assert.notEqual(subject, undefined);
});
