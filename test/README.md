* **/data**: data files for various tests;
* **/int**: integration tests (set of test bound to external env);
* **/man**: manual tests (development stage);
* **/mod**: module tests (functional, with environment);
